# The Busy Business Bits — runtime dependencies
#
# Everything else the app uses (tkinter, urllib, wave, winsound) is stdlib.
# The Anthropic API is called over plain urllib, so no SDK is needed.

# Sprite loading and GIF animation. Required.
Pillow>=10.0

# The "Bits" wake word, and the push-to-talk "mic" button. Optional — without
# these the app says so once and everything else works fine.
SpeechRecognition>=3.10
PyAudio>=0.2.14

# The desk unit (desk/) - an ESP32-2432S028 on USB as a physical approval gate.
# Optional; with no board plugged in the app never notices.
pyserial>=3.5
